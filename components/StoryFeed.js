import React from 'react';
import stories from '../stories';
function StoryFeed() {
  return (
    // <div className="flex items-center border border-black bg-white w-max mx-auto mt-3">
    //   {stories.map((story) => (
    //     <div key={story.id} className="grid items-center py-4 px-3">
    //       <img
    //         className="w-[50px] h-[50px] object-cover rounded-full"
    //         src={story.image}
    //         alt=""
    //       />
    //       <h3 className="ml-3 font-semibold text-sm">{story.title}</h3>
    //     </div>
    //   ))}
    // </div>
    <div></div>
  );
}

export default StoryFeed;
